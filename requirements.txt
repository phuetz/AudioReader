# Core dependencies
kokoro-onnx>=0.4.0
soundfile>=0.12.0
huggingface-hub>=0.20.0

# Audio processing
ffmpeg-normalize>=1.30.0
librosa>=0.10.0
numpy>=1.20.0

# Fallback TTS
edge-tts>=6.1.0

# Web interface
gradio>=4.0.0

# Book export formats
fpdf2>=2.7.0      # PDF generation
ebooklib>=0.18    # EPUB generation

# Optional: Advanced TTS (requires Python 3.10-3.12)
# pip install chatterbox-tts torch torchaudio

# Optional: XTTS-v2 (high-quality voice cloning)
# pip install TTS torch torchaudio

# API Server (for ChatGPT/HTTP integration)
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6

# MCP Server (for Claude.ai integration)
# pip install mcp

# LLM Providers (v5.0 - for LLM Enhancer)
# Ollama (local, free - recommended)
ollama>=0.3.0

# OpenAI (cloud)
openai>=1.0.0

# Anthropic Claude (cloud)
# pip install anthropic>=0.30.0

# Google Gemini 2.5 Flash (cloud)
google-generativeai>=0.8.0

# Development
# pip install pytest black flake8 ruff pre-commit

# v4.0 Dependencies
# Progress bar (optional, enhances CLI experience)
# pip install rich>=13.0.0

# Language detection (optional)
# pip install langdetect>=1.0.9

# Subtitle generation with Whisper (optional)
# pip install openai-whisper

# New TTS engines (optional)
# Chatterbox (Resemble AI voice cloning):
# pip install chatterbox-tts torch torchaudio

# Dia 1.6B (multi-speaker):
# pip install dia-tts torch torchaudio

# F5-TTS (flow matching):
# pip install f5-tts torch torchaudio
